<% if @resource.created_at>1.minute.ago %>

Hello <%= @resource.name %>!

<%= @resource.creator.name %> (<%= @resource.creator.email %>) invited you to use Owl Commander to monitor twilio calls in near real time in a simple and powerful interface. You have already been added to the "<%= @resource.accounts.first.name %>" team.

Your account email address is <%= @resource.email %>

Use the link below to choose a password

<%= edit_password_url(@resource, reset_password_token: @token)%>

<%else %>
Hello <%= @resource.name %>!

Someone (probably you) has requested a link to change your password in Owl Commander. You can do this through the link below.

<%= edit_password_url(@resource, reset_password_token: @token)%>

Once you are logged in, you'll be able to monitor twilio calls in near real time, in a simple and powerful interface.

If you didn't request this, please ignore this email.
Your password won't change until you access the link above and create a new one.
<%end %>
