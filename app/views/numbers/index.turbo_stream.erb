<turbo-stream action="<%= action_for_page(@pagy) %>" target="numbers-rows">
  <template>
    <%= render partial: 'numbers/number', collection: @numbers, locals: { search: @search }, formats: [:html] %>
  </template>
</turbo-stream>

<%= render 'shared/infinite_scrolling' %>
